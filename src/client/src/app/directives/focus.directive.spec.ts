import { FocusDirective } from './focus.directive';

describe('TestDirective', () => {
  it('should create an instance', () => {
    const directive = new FocusDirective(null);
    expect(directive).toBeTruthy();
  });
});
